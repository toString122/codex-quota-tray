{
  "name": "codex-quota-tray",
  "version": "0.1.0",
  "description": "Windows tray utility for showing ChatGPT Codex quota via CLIProxyAPI.",
  "main": "src/main.js",
  "type": "commonjs",
  "bin": {
    "codex-quota-tray": "bin/codex-quota-tray.js"
  },
  "files": [
    "bin",
    "src",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main.js && node --check src/configStore.js && node --check src/preload.js && node --check src/quotaProvider.js && node --check src/usageStatsStore.js && node --check src/trayIcon.js && node --check src/renderer/renderer.js && node --check src/statusbar/statusbar.js"
  },
  "keywords": [
    "codex",
    "quota",
    "tray",
    "electron",
    "windows"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "electron": "^42.2.0"
  }
}
